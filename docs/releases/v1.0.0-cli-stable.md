# v1.0.0-cli-stable

## Release Scope
This release closes the SH-2 path and publishes the first stable CLI-oriented workflow with runtime lifecycle hardening. Scope is limited to the SH-2 chain (`SH-2.1` to `SH-2.4`) and release-readiness artifacts.

## Included Commits
- `b98e045` feat(cli): harden doctor log-dir writability checks
- `4936c00` feat(engine): add events.jsonl lifecycle contract
- `1c8b1f7` feat(engine): persist role execution logs with runtime tests
- `c4bd602` feat: add workspace doctor and repo-root run guard
- `3d5df80` feat: isolate runtime state under workspace .centaur directory

## Validation Evidence
Pre-release verification (executed at repository root `/Users/ender/Workspace/Centaur`):
- `git rev-parse --show-toplevel` -> `/Users/ender/Workspace/Centaur` (RC=0)
- `git status --short` -> clean working tree (RC=0)
- `python3 -m unittest tests.test_engine_runtime` -> `Ran 8 tests ... OK` (RC=0)
- `python3 -m unittest tests.test_cli` -> `Ran 2 tests ... OK` (RC=0)

## Known Risks
- Release push and tag publication depend on remote availability and local auth context.
- `gh release create` is optional and requires GitHub CLI authentication and repo permission.

## Rollback Plan
1. If publication fails before remote push, keep local commit/tag only and fix environment issues.
2. If commit is pushed but release is invalid, push a follow-up correction commit to `main`.
3. If tag must be revoked after push, delete remote tag (`git push origin :refs/tags/v1.0.0-cli-stable`) and recreate with corrected metadata.

## Announcement Draft
`v1.0.0-cli-stable` is now prepared for release. This version finalizes the SH-2 CLI/runtime stabilization path, including workspace guardrails, runtime event lifecycle guarantees, and stronger doctor checks. Validation baseline (`tests.test_engine_runtime`, `tests.test_cli`) is passing. Consumers can adopt this version as the stable CLI baseline.
